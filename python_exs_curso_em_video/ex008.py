number = float(input('Write a number in meters:'))
centimeters = number * 100
millimeters = number * 1000
print('So you wrote {} meters, this is {} centimeters and {} millimeters'.format(number, centimeters, millimeters))
